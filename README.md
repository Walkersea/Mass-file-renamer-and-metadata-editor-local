# Mass-file-renamer-and-metadata-editor-local
A single-file HTML app for mass-renaming files and editing metadata without leaving your browser. Supports find &amp; replace (with regex), prefix/suffix, sequential numbering, case conversion, extension changes, ID3 tag editing for MP3s, and more. 100% offline — nothing ever leaves your machine.
